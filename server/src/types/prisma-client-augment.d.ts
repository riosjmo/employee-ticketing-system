    // // Temporary augmentation: ensure PrismaClient exposes the `file` delegate
    // // This is a safety shim for the editor while the generated Prisma client
    // // types are being refreshed. Once the generated client includes the
    // // `File` model types, this file can be removed.

    // declare module "@prisma/client" {
    //   // eslint-disable-next-line @typescript-eslint/no-explicit-any
    //   interface PrismaClient {
    //     file: any;
    //   }
    // }
    // // End temporary augmentation